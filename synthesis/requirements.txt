# Synthesis track (Text/Speech -> Sign) — in addition to the repo's requirements.txt.
# Install into the same env you use for Recognition.

# Clip playback + stub clip generation + landmark capture.
# Use the CONTRIB build to match the recognition env (mediapipe depends on it), and cap
# below 5.x: opencv 5+ requires numpy>=2, and mediapipe's official wheels hard-require numpy<2
# regardless of the tensorflow version installed. Install only ONE opencv package across the project.
opencv-contrib-python>=4.8,<5
numpy>=1.24,<2.0

# Speech-to-text (ASR) — choose ONE backend; both are optional and lazily imported.
faster-whisper>=1.0        # offline, recommended (downloads a model on first use)
# faster-whisper pulls a newer protobuf that breaks mediapipe — pin it back.
# Transcription still works (via ctranslate2); only faster-whisper's optional onnx VAD needs
# protobuf>=4, which we don't use.
protobuf>=3.20.3,<4
# SpeechRecognition>=3.10  # fallback (online Google API by default); needs PyAudio for mic

# Desktop synthesis demo reuses PyQt5 from the recognition/desktop requirements.
