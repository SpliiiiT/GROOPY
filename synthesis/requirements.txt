# Synthesis track (Text/Speech -> Sign) — in addition to the repo's requirements.txt.
# Install into the same env you use for Recognition.

# Clip playback + stub clip generation + landmark capture.
# Cap below 5.x: opencv-python 5+ requires numpy>=2, which conflicts with tensorflow 2.15
# (numpy<2). Keep 4.x so both tracks share one env.
opencv-python>=4.8,<5
numpy>=1.24,<2.0

# Speech-to-text (ASR) — choose ONE backend; both are optional and lazily imported.
faster-whisper>=1.0        # offline, recommended (downloads a model on first use)
# SpeechRecognition>=3.10  # fallback (online Google API by default); needs PyAudio for mic

# Desktop synthesis demo reuses PyQt5 from the recognition/desktop requirements.
