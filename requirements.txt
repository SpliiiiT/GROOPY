# Core deep learning
tensorflow==2.15.0        # use tensorflow (GPU build) on Colab / local CUDA
numpy>=1.24,<2.0
pandas>=2.0

# Landmark extraction + vision.
# Use the CONTRIB build (mediapipe depends on it) and cap below 5.x — opencv 5 requires
# numpy>=2, which conflicts with tensorflow 2.15 (numpy<2). Install only ONE opencv package.
mediapipe==0.10.9
opencv-contrib-python>=4.8,<5

# Evaluation, plotting, XAI
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
tf-explain>=0.3.1         # Grad-CAM

# Dataset download
kaggle>=1.6

# Desktop app (install separately if you only want the app: see desktop/requirements.txt)
PyQt5>=5.15

# Export helpers
tensorflowjs>=4.17        # optional: web export. Pin may need matching TF version.
