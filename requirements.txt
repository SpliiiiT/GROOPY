# Core deep learning
tensorflow==2.17.1        # Keras 3 (fixes the local Keras-2-vs-Colab-Keras-3 load mismatch this
                          # project hit). Do NOT pin this same version in 02_bakeoff.ipynb's Colab
                          # cell — that was tried, and forcing numpy<2 there breaks ABI
                          # compatibility with Colab's own preinstalled numpy-2.x-compiled packages
                          # (jax, pandas, ...). Local and Colab no longer need to match exactly: both
                          # are on Keras 3 now, and Keras 3's save format is compatible within the
                          # 3.x line, so Colab can just use its own default TF.
keras==3.15.0             # pinned explicitly (not just via tensorflow's floor) to avoid drifting to a
                          # newer 3.x on a future local reinstall.
numpy>=1.24,<2.0
pandas>=2.0

# Landmark extraction + vision.
# Use the CONTRIB build (mediapipe depends on it) and cap below 5.x — opencv 5 requires numpy>=2,
# and mediapipe's official wheels hard-require numpy<2 (independent of the tensorflow version
# chosen). Install only ONE opencv package.
mediapipe==0.10.9
opencv-contrib-python>=4.8,<5

# Evaluation, plotting, XAI
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12

# Dataset download
kaggle>=1.6

# Desktop app (install separately if you only want the app: see desktop/requirements.txt)
PyQt5>=5.15

# Export helpers
tensorflowjs>=4.17        # optional: web export. Pin may need matching TF version.
